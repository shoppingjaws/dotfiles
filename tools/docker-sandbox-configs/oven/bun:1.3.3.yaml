# Bun runtime configuration for docker-safe-sandbox
image: oven/bun:1.3.3
workdir: /workspace

volumes:
  - host: ./node_modules
    container: /workspace/node_modules
    readonly: true

environment:
  NODE_ENV: production
